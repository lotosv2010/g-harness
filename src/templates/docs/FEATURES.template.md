# 功能清单（FEATURES）

> 由 `gforge index` 自动生成。功能名 → 入口 + 相关文件的映射，便于 AI 快速定位。
> 生成时间：（首次生成时由 CLI 填充）

_运行 `gforge index` 生成内容。_

## 如何扩展

- 新增功能：先更新 SPEC.md 的功能需求，再实现并运行 `gforge index` 刷新
- Bug 修复：在 FEATURES 中查找功能对应入口，缩小定位范围
- 功能名与模块名不一致时，以本索引为准
