# 项目地图（PROJECT_MAP）

> 由 `g-harness index` 自动生成。AI 在改动前应优先阅读本文件，避免整体扫描源码。
> 生成时间：（首次生成时由 CLI 填充）

## 模块清单

_运行 `g-harness index` 生成内容。_

## 使用指南

- 修改某个模块前：先读该模块的入口文件（`index.ts`）了解公共 API
- 新增模块：遵循 ARCHITECTURE.md 模块划分约束，更新本索引
- 索引过期：运行 `g-harness index` 重新生成

## 维护约定

- 不要手动编辑本文件，所有变更通过 `g-harness index` 重新生成
- 新增大模块时同步更新 ARCHITECTURE.md 的模块划分章节
- 索引文件应纳入版本管理，确保团队成员看到一致的项目视图
