# 数据模型规格

> 由 G-Forge 生成。定义项目核心数据结构。
> AI 在创建或修改数据相关代码时必须参考本文件。

---

## 1. 核心数据模型

### 1.1 [模型名称]

```typescript
interface Example {
  id: string
  name: string
  createdAt: Date
  updatedAt: Date
}
```

（请根据项目实际数据结构补充）

## 2. 持久化方案

| 数据 | 格式 | 存储位置 |
|------|------|----------|
| 上下文 | Markdown | `CLAUDE.md`、`AGENTS.md` |
| 配置 | JSON / YAML | 项目根目录 |
| 运行时数据 | （请补充） | （请补充） |

## 3. 数据流

```
（请描述数据在系统中的流转路径，如：
输入 → 解析/验证 → 处理/转换 → 持久化 → 输出）
```

## 4. 数据校验规则

| 字段 | 规则 | 说明 |
|------|------|------|
| | | |
