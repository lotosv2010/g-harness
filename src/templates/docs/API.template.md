# API 契约定义

> 由 G-Harness 生成。定义项目的 API 接口规格。
> AI 在开发或调用 API 时必须参考本文件。

---

## 1. API 概览

（请根据项目需要补充 API 定义）

## 2. 接口定义

### 2.1 [接口名称]

```
方法：GET / POST / PUT / DELETE
路径：/api/v1/...
```

**请求参数：**

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| | | | |

**响应格式：**

```json
{
  "code": 0,
  "message": "success",
  "data": {}
}
```

**错误响应：**

```json
{
  "code": 40001,
  "message": "参数错误",
  "data": null
}
```

## 3. 错误码

| 错误码 | 含义 | HTTP 状态码 |
|--------|------|-------------|
| 0 | 成功 | 200 |
| 40001 | 参数错误 | 400 |
| 40101 | 未授权 | 401 |
| 50001 | 服务器内部错误 | 500 |

## 4. 认证方式

（请补充认证方案：JWT / Session / API Key 等）
