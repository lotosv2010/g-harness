{
  "$schema": "2",
  "name": "base",
  "label": "通用基础",
  "description": "技术栈无关的基线规范，适合不匹配任何预设的场景",
  "techStack": {},
  "variables": {
    "shared_dir": "src/shared"
  },
  "commands": {},
  "architecture": {
    "overview": "按职责分层；避免循环依赖；测试与实现邻近",
    "structure": "src/\n└── <按职责拆分的模块>"
  },
  "modules": ["core", "shared"],
  "rules": [
    "关注单一职责，单文件不超过 300 行",
    "优先命名导出，避免默认导出",
    "异步错误必须被处理或显式传播"
  ],
  "nfr": ["关键路径需具备可观测性与可回滚手段"]
}
