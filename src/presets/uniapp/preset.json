{
  "$schema": "2",
  "name": "uniapp",
  "label": "uni-app",
  "description": "uni-app Vue 3 + TypeScript 多端统一开发",
  "techStack": {
    "language": "TypeScript",
    "framework": "uni-app Vue 3",
    "packageManager": "pnpm"
  },
  "detect": {
    "dependencies": ["@dcloudio/uni-app"],
    "files": ["manifest.json"]
  },
  "variables": {
    "shared_dir": "src/utils",
    "routes_dir": "src/pages"
  },
  "commands": {
    "dev": "pnpm dev:mp-weixin",
    "build": "pnpm build:mp-weixin"
  },
  "architecture": {
    "overview": "Pages 路由 + Components 复用；条件编译隔离多端差异",
    "structure": "src/\n├── pages/\n├── components/\n├── store/\n└── utils/"
  },
  "modules": ["pages", "components", "store", "utils"],
  "rules": [
    "平台差异通过 #ifdef / #ifndef 条件编译，禁止运行时 hack",
    "状态集中到 Pinia store，避免跨页直接读写 globalData",
    "图片路径统一使用 /static/ 前缀"
  ],
  "nfr": ["关键页面使用 onLoad 异步预取数据，避免闪动"]
}
